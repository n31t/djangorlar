[pytest]
DJANGO_SETTINGS_MODULE = djangolar.settings.base
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --strict-markers
    --tb=short
    --reuse-db
markers =
    django_db: mark test as requiring database access
